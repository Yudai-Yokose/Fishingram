<%= turbo_frame_tag "comment_form_#{catch.id}" do %>
<section class="bg-gray-50 dark:bg-gray-900">
  <div class="flex flex-col items-center justify-center px-1 py-1 mx-auto">
    <div class="w-full bg-white rounded-lg shadow dark:border sm:max-w-md dark:bg-gray-800 dark:border-gray-700">
      <div class="p-6 space-y-4">
        <!-- Title -->
        <h1 class="flex items-center justify-center mb-2 text-lg text-gray-900 dark:text-white">
            <%= image_tag "/2.svg", class: "w-8 h-8 mr-2", alt: "logo" %>
            <%= t('activerecord.attributes.comment.new.title') %>
            </a>
        </h1>

        <!-- Form -->
        <%= form_with(model: [catch, catch.comments.new], html: { class: "space-y-2 md:space-y-6" }) do |f| %>
            <%= f.text_area :content, class: "bg-gray-50 border border-gray-300 text-gray-900 text-base rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 text-gray-500 dark:text-gray-400 dark:focus:ring-blue-500 dark:focus:border-blue-500" %>
            <!-- Submit button -->
            <%= f.submit t('activerecord.attributes.comment.new.submit'), class: "w-full text-white bg-primary-600 hover:bg-primary-700 focus:ring-4 focus:outline-none focus:ring-primary-300 font-medium rounded-lg text-base px-5 py-2.5 text-center dark:bg-primary-600 dark:hover:bg-primary-700 dark:focus:ring-primary-800" %>
        <% end %>
      </div>
    </div>
  </div>
</section>
<% end %>