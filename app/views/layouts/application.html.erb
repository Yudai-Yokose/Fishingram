<!DOCTYPE html>
<html class="dark">
  <head>
    <title><%= content_for(:title) || "Fishingram" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon_and_text_navy_bg.png" type="image/png">
    <link rel="icon" href="/icon_and_text_navy_bg.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon_and_text_navy_bg.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <%= display_meta_tags(default_meta_tags) %>
  </head>

  <body class="bg-gray-900 text-white">
    <div id="flash"></div>
    <%= render 'devise/shared/error_messages' %>
    <%= render "shared/flash" %>
    <%= render "shared/header" %>
    <%= yield %>
    <%= render "shared/footer" %>
  </body>
</html>