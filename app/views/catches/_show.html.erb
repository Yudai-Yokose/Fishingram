<div class="max-w-sm bg-white border border-gray-200 rounded-lg shadow dark:bg-gray-900 dark:border-gray-800" data-action="click->hello#stopToggle">
    <div id="catch_<%= catch.id %>_images">
        <%= render 'images', catch: catch %>
    </div>

    <div id="catch_<%= catch.id %>_details">
        <%= render 'details', catch: catch %>
    </div>
</div>