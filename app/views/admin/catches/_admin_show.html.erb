<div class="max-w-sm border rounded-lg shadow bg-gray-900 border-gray-700" data-action="click->hello#stopToggle">
    <%= turbo_frame_tag "admin_catch_show_#{catch.id}_images" do %>
        <%= render 'admin_show_images', catch: catch %>
    <% end %>

    <%= turbo_frame_tag "admin_catch_show_#{catch.id}_details" do %>
        <%= render 'admin_show_details', catch: catch %>
    <% end %>
</div>